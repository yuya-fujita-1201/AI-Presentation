# loop-learnings: presentation（デッキ・ナレッジの品質ループ）

ループエンジニアリング／グラフエンジニアリングで成果物を磨くときに、**次回のループに効くことだけ**を蓄積するファイル。
ループ開始時（Step 0）に読み、終了時に1〜3行だけ追記する。周回の経緯・日時・スコア推移は各 loop-log.md に、成果物の中身はコミットに任せる。

運用:
- 同じ内容が3回溜まったら「統合済み原則」に1行で畳み、元の行は消す
- 「機械ゲートに昇格すべきもの」は状態列を持つ。**人間レビューで「これは機械で見られる」と判断したものは、ここに書いた時点でゲート化の候補**になる
- 採点軸を入れ替えると過去の合格品が不合格になりうるので、四半期に一度は軸の棚卸しをする（この節の末尾に棚卸し日を残す）
- 初版（2026-08-20）は、2026-08-09〜08-19 のループ記録・人間レビュー・パイプライン採点56本・improve対応276件から蒸留した

---

## 1. rubric に足すべき／直すべき観点

- 「人間らしい文章」は **語句レベル**（定型句・ダッシュ・ヘッジ語尾）と **構成レベル**（「AではなくB」型の反復、punch と caption の同一主張二段反復、体言止めの連打）を分けて書く。分けないと採点者交代で着眼点が変わりスコアが逆転する（tone ループ: 語句は全解消済みなのに 41→36）
- 「シリーズ整合（04の直後に流しても同じ人が作ったように見えるか）」は最初から独立項目にする。05では後付けになり、さらに横断 rubric を別に作る羽目になった
- 比較基準となる **合格済み正本デッキ（01）を rubric に名指し**する方式は3デッキで有効だった。「合格ラインは01の文体と刻み」
- 想定読者を「AI初心者・技術力が高くない人」と明記し、「減点理由を1つでも挙げられれば満点にしない」「自分（AI）が理解できるかは基準にしない」と書くと、実態に近い低いベースラインが出る（04: 19/50）
- 過去に実際に起きた事故名を観点に書いておくと再発検知に使える（CE: 「旧8・9枚目のプロンプトの意味の矛盾の再発禁止」）
- 事実正確性には「伝聞・聞き取りである旨が明記されているか」「条件を落として一般化していないか」「auto字幕のみ根拠の断定が中核に混じっていないか」を含める
- 「用語の意味がスライド間で暗黙に切り替わっていないか」（プロンプト＝入力の総称／1回の頼み方、の揺れ）を独立の観点にする。CEの根本フィードバックの原因だった
- 「読後に最低1つ試せるか」（置き換え箇所を［　］で示したテンプレ＋「返ってきたら人が確認する」の一文）は実用性評価に直結。3デッキで繰り返し確認
- 採点者の指摘も誤る（比喩「宿題のチェック」の創作疑いは誤検出だった／ダッシュ冒頭配置の指摘も事実誤認）。**採点指摘は実物と突き合わせてから直す**を rubric 側の採点者指示にも書く（「再現箇所を必ず付ける」）
- 「初心者への適合」は採点者が最も割れる項目（02 で 45/94/65、04 で 65/95/75）。割れる原因は「誰を初心者とするか」の解像度不足。rubric には読者像を **具体の語彙**で書く（例: 「npm test・コミット・フックが読めない読者」）。spread ≥25 の項目は次の棚卸しで観点を具体化する（2026-08-20 v2 採点で確認）
- findings 駆動（点を書かせず 大25/中5/小1 で計算）にすると、5デッキ×9項目=45セルのうち 35 セルで3名のスプレッドが 10 点以内（100点満点）に収まった。割れたのは「初心者への適合」（全5デッキで 18〜49）と「図解」（02/03/04 で 19〜30）に集中。**印象点より列挙の方が安定し、割れる項目＝観点が曖昧な項目がはっきり見える**（2026-08-20 第1周）

## 2. 減点の実体（rubric 項目名 → 実際に減点されたパターン）

rubric の観点名は抽象的なので、採点者が実際に何で減点したかを残す。減点基準（大・中・小）を明文化する材料。

**デッキ（grade_d 17本・findings 57件）**
- ビジュアル品質（平均7.4・17本中9本が未達、最弱）: 折り返しで1〜2文字の孤立行／要素の重なり（見出しと注記が同位置）／Markdown記法（`や##）の残留／フッター欠落／表の最終行がフッターに食い込む
- 余白・密度: 下半分が空白／punch が45〜60字で2行折り返しが常態化（16枚）／隣接スライドの内容重複
- 超初心者への配慮（平均7.7）: 専門語の初出無説明（「サブエージェント」「schema」「checksum」「正本」）／図だけ英語ラベル
- 内容の正確性: デッキ独自の出典番号が sources/ 台帳に対応ファイルなし（14件中9件）
- ストーリー: 章扉の予告枚数と本文枚数の不一致／「N枚目」直書きが付録追加でずれる
- スピーカーノート: ほぼ減点なし（観点として機能していない可能性。棚卸し候補）

**ナレッジ（grade_k 39本・findings 304件）**
- 文章品質（平均6.9・39本中36本が未達、最弱）: 同一主張の複数ファイル全文再掲（39件）／生成世代間の文体二層化（箇条書き主体 vs 断定一文段落＋太字）／定型句反復（「〜が効く」「出所の異なる〜が一致」）
- リンク（60件、最多）: 台帳「活用先」⇄コンセプトのインライン出典が片方向／Citations 欠落／本文に出典リンクが無く末尾まで戻らないと出所に辿れない
- 網羅性（49件）: 動画の中核セット（具体例・運用手順・数値）がどのコンセプトにも居場所がない
- 事実正確性（34件）: auto字幕由来を事実断定／ソース間矛盾の誤統合（「最大16」vs台帳「64」を「複数ソースで一致」と書く）／出所の性格づけ誤り
- 構成（34件）: 1ファイルに11トピック／予告と本文の不一致／「次に読む」の欠落・循環

## 3. 機械ゲートに昇格すべきもの

状態: ✅実装済み ／ 🧪試作済み（`tools/` にあるが gate 未組込） ／ ⏳候補

| 状態 | 欠陥 | 事故の実績 | 検出ロジック |
|---|---|---|---|
| ✅ `tools/check_layout.py`（gate_deck exit 16） | 文字の箱からの溢れ・ステージ外・文字同士の重なり・割当高さ超過（closing 2行化）・罫線との交差 | closing タイトル19→32字で罫線貫通（7周検出されず）、S30/S37 の枠超過、RAG S34 の表がフッターに重なる。計5回以上 | HTML を Playwright で開き DOM 矩形を実測。装飾1文字（引用符）は除外、spill は 12px 超で不合格・以下は警告。良品デッキ 01〜05 で誤検出 0 に較正済み（2026-08-20） |
| ✅ `tools/check_svg_fonts.py`（exit 17） | SVG の text 要素が明朝体にフォールバック（`.t` クラス任せ） | 2026-08-14 に6箇所、2026-08-20 に **01正本の3図版16要素**（slide-23 で実表示確認、修正済み） | SVG を Chromium で開き computed font-family を全 text で検査 |
| ✅ 04 `generate_assets.py` のみ | SVG 図中ボックスの幅超過（日本語化で幅未検証） | 3ボックスが枠を越えた（9字×19px > 162px） | 日本語1文字≒font-size で幅見積もり、超過で SystemExit。他デッキへ横展開が未了 |
| ✅ `tools/lint_deck_text.py`（exit 18） | closing タイトル22字超／Markdown 記法残留／「N枚目」が総枚数超過 | 上記の罫線貫通（04 で `overflow_check.py` を scratchpad に作成→消失） | deck.json の表示フィールドを走査。AI定型句・「ではなく」・ダッシュ・丸数字・半角コロン・ページ番号直書きは**警告**として件数を出す（採点者の減点材料） |
| ⏳ | `image_text` の画像枠下端とキャプション y の衝突 | 07-okf-visual と 05 で独立に2回 | レイアウト定義の座標差分チェック（check_layout の overlap でカバーできるか要確認） |
| ⏳ | 「N枚目」直書き参照のずれ | 05 S39「45枚目」が実体48枚 | 「\d+枚目」を grep し総数・実タイトルと照合、または直書き禁止 |
| ⏳ | 専門用語の初出無説明（用語アンカー） | 04 で `jargon_check.py` を作り SVG にも拡大したが **scratchpad に置いたため消失** | 用語リスト×初出スライドで「説明語が同一スライドにあるか」。SVG 内テキストも対象 |
| 🧪 lint_deck_text の警告 | AI定型句・「AではなくB」型・全角ダッシュ「——」・①②③の多用 | tone ループ、04（53枚中23箇所）、03（「ではなく」19回→maker が16回に） | 件数は出るが閾値で落とすのは未実装（正本01でも「ではなく」5回・丸数字19個あり、閾値は正本で較正してから） |
| ❌ 却下 | punch の文字数（35字超で2行化） | grade_d で16枚が常態化 | **正本01の punch は中央値46字・最大80字で2行折り返しが設計**。長さ自体は減点にならない。lint は 80 字で警告のみ |
| ✅ lint_deck_text | Markdown 記法の残留（`、##、**） | grade_d で検出 | 表示フィールドを走査（code フィールドは除外。テンプレの `#` 見出しは正当） |
| ✅ lint_deck_text（警告） | 半角:／全角：の混在 | 01 v2 レビュー slide10・27 | 日本語文中の半角コロンを警告 |
| ⏳ | 出典表の URL・番号が台帳に未登録 | 03 で14件中9件 | deck.json の出典 ⇄ knowledge/sources/ frontmatter の URL 突合 |
| ⏳ | ナレッジの活用先⇄Citations の双方向不一致 | grade_k 最多60件。採点者は手動で15件と過小報告、スクリプトでは16件 | リンク元/先集合の双方向比較 |
| ⏳ | 同一主張の複数ファイル全文再掲 | grade_k 39件 | 段落単位のハッシュ／類似度で重複検出 |
| ✅ check_layout の overlap | 表の行数×行高がフッター y を越える | RAG S34（2026-08-20 検出、パイプライン側で未修正） | DOM 実測で検出済み |
| ⏳ | SVG 生成スクリプトのフッター幅ガード | 04 `generate_assets.py` の compare()/flow()/grid()/cycle() のフッターに `_fit` が無く、文言変更で箱からはみ出す回帰を1回作った（2026-08-20） | 生成関数のフッターにも `_fit` を通す。check_layout は SVG 内部を見ないので生成側で守る |
| ⏳ | 生成スクリプトが手修正 SVG を上書き | 04 `diagram-series-five-layers.svg` は手修正版で、`generate_assets.py` をフル実行すると消える（2026-08-20 に2回踏んだ） | 生成対象から除外するか、生成後に差分確認を強制する |
| ✅ | notes_slide 混入・枚数不一致・PPTX 破損・プレースホルダー語句 | — | gate_deck.sh／PPTX混入検査。他項目のロールモデル |

## 4. 読者（AI初心者・非エンジニア）が理解しやすい／しづらい書き方

**しづらい**
- 用語先出しの無説明（エージェント・リンター・型チェック・ワークツリー・rubric・PASS）。初心者適合が 3/10 まで落ちる
- 流行語の起源やジョーク説から入って定義が11枚目まで出ない（05初版）
- コード／Claude Code 偏重の例。非エンジニアは「自分の仕事の話」と思えない（事例 2/10）
- 図だけ英語ラベル（本文は易しいのに図が難しい）。4回指摘
- 「AではなくB」の対句連打・体言止めの警句・punch と caption の同一主張反復 → 「AI臭い」で一貫して低評価
- 同じ概念に別々の比喩／比喩が説明対象と構造対応していない
- 抽象語・意味の取りにくい比喩（「空欄」→「足りない情報」に置換させられた）
- 前方参照（未説明概念に依存する説明）。章扉の橋渡し文で解消できる
- 断定しすぎ（聞き取り・推定を事実のように書く）。Codex反証レビューで繰り返し指摘

**しやすい**
- **bullets は「太字ラベル＋子項目」の二階層**にする。01/02 はほぼ全スライドがこの構造で、読者は太字だけ流し読みできる。04/05 はフラットな完結文5本（1本26〜31字）に退化していて1行が重い（2026-08-20 差異分析）
- **punch は「このスライドの結論」を一文で言い切る**（01/02 は全 punch がです・ます体の結論）。caption 等の2つ目の短文を置くなら、punch の言い換えにせず「橋渡し・予告」専用にする（05 で反復を確認）
- **比喩はシリーズ横断で同一フレーズ**を使い回す。「AIは優秀な新入社員」は 01→02 で継続、03〜05 で断絶していた（grep で 0 回）。断絶すると各回ゼロから比喩を作り直すことになる
- **専門用語は表の見出しでも日本語を主**にする。英語見出しに日本語の言い換え列を添えるだけでは翻訳したことにならない（03 の責務表）
- **配布用テンプレ（code スライド・［　］埋め）を各デッキに最低1枚**。01=11・02=4 に対し 03=1・04=0・05=0 で、実用性の差がそのまま出た
- 章が長い（50枚超）デッキは、章末に早見表1枚で章内容を回収する（02 S19 型）。closing だけに回収を委ねると読者が迷子になる
- 冒頭は 表紙 → 5層マップ（全体地図）→ 学べること → 章扉。「今回はどの層の話か」が2〜3枚目で分かる
- 定義ファースト: 用語の意味 → 具体例、の順。本編冒頭7枚以内で「結局何なのか」に答える
- 難所（抽象論の直後）に業務例（経費精算・議事録・稟議・アンケート）を置く（事例 2→9）
- 悪い例／良い例は左右比較（two_column）で
- 抽象関係（プロンプトとコンテキストの包含）は表より包含図
- 「優秀な新入社員への引き継ぎ」比喩はシリーズ横断で同一フレーズのまま使う
- notes に話者の言い淀み（「だと思います」「正直」）を残すと人間らしさの評価が上がる（05: 18箇所で04より高評価）。ただし効くのは 05 のように長め（250字超）で一人称・聴衆への呼びかけを伴う notes。01〜04 の事実列挙型 notes（100〜160字）に言い淀みだけ足しても効果は薄い。これは **正本 01/02 が 05 から学ぶ側** の唯一の軸（2026-08-20）
- テンプレは［　］で置き換え箇所を示し、「返ってきたら人が確認する」を添える
- 出典はスライドに散らさず最終ページ・付録に集約

## 5. 効いた打ち手

- maker ≠ grader（経緯を渡さない別エージェント）は全ループで機能。4回
- 採点者を2名以上立て、項目ごとに低い方を採る／並列で走らせる。ばらつきの影響が緩和（04・03-05横断）。2回
- 採点者の完了通知サマリと本文が食い違う（40→実際36 など3件）。**本文を正とし、機械照合できる指摘はスクリプトで裏取り**してから直す。4回
- グラフ（複数観点の並列採点でベースライン把握）→ ループ（反復改善）の2段構成。04・05で採用
- アンカースクリプトは1周目で作って終わりにせず、見落とし（SVG非対応・過検出）を踏まえて毎周強化する
- 独立ファクトチェッカーに一次資料（arXiv・公式ブログ）を直接当てさせると、動画由来の誤要約を1周で検出（05: 論文に存在しない対比軸）
- 文体の改善は、先に正本（01）の文体アンカー（punch の文末型・対比構文数・caption 数）を実数カウントしてから直すと速い
- **3名並列・findings 駆動・レンズ分け**（初心者通読／文章文体／事実図解実用）で、5デッキのベースラインが1回で揃った（各デッキ 20〜36 件の統合 findings、03/04/05 は maker の rejected 0）。採点者には全スライドのコンタクトシート（6枚/画像）＋ deck.json を渡せば十分で、PNG 1枚ずつの実見より安い
- maker に「統合 findings ＋ 正本との差異分析 ＋ loop-learnings §4」を渡し、**大→中→小・agree 順**で直させると、1周で未達項目が解消する見込みが立つ。maker は findings を実物と突き合わせて rejected にできる（01 では 23 件中 11 件を正本保護の理由で却下）
- 正本（01/02）に手を入れるときは「文体・刻み・構成は変えない。大・agree≥2・明確な実害だけ」と maker 指示に書く。書かないと好みの言い換えが混ざる
- デッキの定量指標を横並びで出す（`tools/deck_metrics.py`）と、差異が数字で見える（03 の英字語330・表27枚、04/05 の bullets 1本26〜31字、05 の caption 27、code スライド 11/4/1/0/0）。採点者の印象より先にこれを見る

## 6. 効かなかった／回帰の原因

- **合格後の軽微な追加編集が新しいレイアウト回帰を生んだ**（S40 罫線重なり・S30 はみ出し）。合格後の追撃はループを通すか、ユーザー判断に委ねる
- SVG ごとに「最長テキストだけ」書き換える一括修正は、同長の別テキストを取りこぼす（警句が7周残った）
- 図の英語ラベルを日本語化するとき幅を再検証しないと枠を越える
- はみ出しアンカーの初版が厳しすぎ、正常な2行折り返しの章扉まで不合格にした。**許容値は良品デッキで較正**してから使う
- `generate_assets.py` が旧文言のまま放置され、実行すると手修正65箇所が消える状態だった。SVG を手で直したら生成スクリプトも追従させる
- 04 で作った機械アンカー3本（voice/jargon/overflow）は scratchpad に置いたためセッション終了で消え、05 以降で再利用できなかった。**アンカーは `tools/` に置いてコミットする**
- improve 工程の「findings の where が指すファイル以外は触れない」制約で、正しい指摘が deferred(out_of_scope) になり、同じ論点が複数 run で繰り返し検出された（deferred 15件中14件がこれ）。採点者に許可範囲を知らせるか、improve の許可範囲を findings 単位で広げる
- 5項目同時に目標到達しない「ばらつき」が主因で manual_override（未達のまま受理）が ge/he/le/ce 全テーマで発生。ge は9周回しても同時達成せず
- rubric-deck テンプレの「スピーカーノート」項目は17本中0本で減点。観点として仕事をしていない（入れ替え候補）
- **同一作業ツリーで maker を並列させたら、1体の `git stash`（警告が既存か自分由来かの切り分け目的）で他の maker の未コミット変更まで全部退避された**（2026-08-20 01:47、stash pop で復旧）。maker 指示書に git 操作禁止を明記し、「既存の警告」はベースラインのゲート出力をファイルで渡して比較させる。デッキ単位でゲートが通り次第すぐコミットする
- 枚数を固定する `tools/verify_ai_eng_series.py` の TARGETS は、並列 maker が同時に編集しうる共有ファイル（隠れたエッジ）。枚数変更は maker に申告させ、統合担当がまとめて直す方が安全

## 7. 運用上の教訓（パイプライン・ブランチ）

- 自律パイプラインは **現在のブランチに** コミットする。並行で手作業する前に `pipeline/PAUSE` を置く。他レーンへの誤爆・未コミット修正7件の巻き戻し・未追跡SVG2点の削除が実際に起きた。**2026-08-20 にも再発**（PAUSE 前に作った `tools/check_svg_fonts.py` が improve_d run に隔離された）。ファイルを1つでも作る前に PAUSE
- 対象パスを限定したコミット（デッキ配下だけ）で、無関係な未コミット変更が混在する作業ツリーでも安全に作業できた。3回
- 「復元したベースライン」が別ブランチにしか完全体がないことがある。新規作成前に未マージPRブランチを確認する
- Codex への依頼書は「変更禁止」範囲を列挙し「気づいた文章問題は直さず報告のみ」と権限を区切ると越境しない

## 8. 統合済み原則

- 機械で測れるものは rubric から機械ゲートへ移す。人間レビューで同じ指摘が2回出たら、その時点で昇格候補に載せる
- 採点者・検証エージェントの指摘は、実物と突き合わせてから直す（サマリより本文、本文よりスクリプト）
- 合格済みの正本を「比較基準」として rubric に名指しする
- 採点は複数名・findings 駆動・レンズ分け。点は列挙から計算する（印象点にしない）

## 棚卸し記録

- 2026-08-20: 初版作成（過去記録からの蒸留）
- 2026-08-20 深夜: 01〜05 再レビュー（v2 採点・3名並列・機械ゲート3本追加）の結果を反映。§3 の状態更新、§4 に二階層 bullets／punch＝結論／比喩継続／表見出し日本語／配布テンプレ／章末早見表を追加。次回棚卸し目安: 2026-11
