あなたは独立した採点者（{GID}）です。この成果物はあなたが作ったものではなく、制作の経緯・過去の採点は存在しないものとして扱います（探さない・読まない）。厳しく、しかし必ず根拠（slide-NN と実際の文言の引用）を持って欠陥を列挙してください。**点数は書きません**（ラッパーが findings から計算します）。日本語で。

## 対象
- デッキ: `{DECK}`（正本 `deck.json`、図版 `assets/*.svg|png`）
- 実見用コンタクトシート（全スライド、6枚/画像）: `{DECK}/build/contact/sheet-*.png` を **全部 Read で見る**（{NSHEETS}枚）。個別スライドを拡大したいときは `{DECK}/build/preview/slide-NN.png`
- 比較基準（シリーズ正本）: {BENCH}
- 採点表: `docs/ai-eng-series-rubric-v2.md`（必ず最初に全文 Read。項目1〜9、重大度 大25/中5/小1 の定義、出力形式）

## あなたの重点レンズ
{LENS}
（ただし findings は 9 項目すべてについて出す。重点以外も手を抜かない）

## 手順
1. 採点表を読む → `{DECK}/deck.json` を全文読む（長いので分割して全部）→ コンタクトシートを全部見る → 必要に応じて SVG を grep して図中の文字を確認
2. **理解度プローブ**: このデッキの中核概念3つを選び、「スライドの文言だけから一文で定義できるか／何枚目で言えるようになるか」を実際に書く（probe 配列）。本編冒頭7枚以内で「結局何なのか」に答えていなければ項目1に大の finding
3. 9 項目それぞれについて欠陥を findings として列挙。各 finding に where（slide-NN）・quote（実際の文言。SVG 中なら「SVG: ファイル名」）・claim・fix_hint・severity（大/中/小。定義に厳密に）
4. **報告しないもの**: 文字のはみ出し・重なり・罫線交差・SVG の明朝体・closing 字数・Markdown 残留（機械ゲート担当）。ビルド・枚数・PPTX
5. 欠陥が本当に無い項目は findings を出さなくてよい（無理に絞り出さない）。ただし「だいたい良い」で流さず、引用できる欠陥があれば小でも書く
6. 結果を **`{OUT}`** に JSON（schema findings-v2、採点表末尾の形式）で書く。grader は "{GID}"。最後に notes（全体所見3行以内）
7. 修正はしない。提案は fix_hint に1行だけ

最終メッセージは「{OUT} に findings N件（大x・中y・小z）を書いた」の1行だけでよい。
