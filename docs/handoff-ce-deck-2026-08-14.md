# CEデッキ ハンドオフ（2026-08-14 作成）

次のスレッド／セッションが `decks/ai-eng-02-context-engineering`（コンテキストエンジニアリング超入門）の作業を引き継ぐための文書。前提知識ゼロで読めるように書いてある。

## 1. いまの現在地（最重要）

- **ユーザーが見る場所は `decks/ai-eng-02-context-engineering-v2/` だけ**（main チェックアウト直下・未コミットの閲覧用コピー）。中身は「2〜4章・付録の全面再構成（71枚）＋9枚目改稿」を合流した最新統合版。閲覧は `build/ai-eng-02-context-engineering.html`
- **git上の正本はPRブランチ側**。v2フォルダを直接編集しない。修正は「ブランチで deck.json 修正 → ビルド → v2 へコピー」の順（詳細は §5）
- `decks/ai-eng-02-context-engineering/`（git管理中）は**マージ前の旧58枚版**。触らない

## 2. オープンなPRと競合状態

| PR | 内容 | 状態 |
|----|------|------|
| #4 | 1章の流れ整理と7・8枚目図解化 | OPEN |
| #5 | 旧10枚目→7枚目へ移動し「前提情報の3段階（都度・条件付き・必ず）」化 | DRAFT |
| #6 | **2章のみ**再構成（5項目廃止、ルールファイル/メモリ/設定ファイル/スキル構成、58枚維持）別セッション作 | OPEN |
| #7 | **2〜4章＋付録の包括再構成（58→71枚）**。本ハンドオフの主成果 | DRAFT |
| #8 | 9枚目を新入社員vsベテラン対比へ刷新 | DRAFT |
| #2 | 初版の古いdraft。実質不要（クローズ候補、ユーザー確認後に） | DRAFT |

- **未解決の裁定: #6 と #7 は2章の設計が競合**。#6=5項目廃止・58枚維持／#7=5項目を「プロンプト経路の道具」として保持し3〜4章・付録まで一貫再構成。**どちらを採るかはユーザー判断待ち**。#7採用なら#6クローズ、#6採用なら#7の2章を作り直し
- **推奨マージ順: #4 → #5 → #7**（deck.jsonの隣接領域を触るためコンフリクト時はリベースで解消）。#8は範囲非干渉
- #5 が入らない場合のみ、1章S10の用語（能動/自動/必要時）を2章の「都度/必ず/条件付き」に合わせる1枚修正が必要（採点で残った唯一の8点指摘）

## 3. #7 の設計意図（ユーザーの包括レビュー 2026-08-14 の実装）

ユーザーの前提: **コンテキスト＝①プロンプト（都度渡す）②ルールファイル（必ず読み込まれる）③特定条件の時に渡るもの（スキル・検索・コネクター）**。この三分法を先に示し、その後に各置き場所の最適化を語る——5項目などの各論を先に出さない。これが再構成全体の背骨。

- **2章 基礎編（S11〜20）**: 章地図（三分法の再掲）→ プロンプト最適化（5項目・議事録Before/After・テンプレ）→ ルールファイル（仕組み→CLAUDE.md実物→書く内容4分類）→ 条件付きの仕組み（例示のみ）→ 早見表。RAG・コネクター詳細は3章へ送る
- **3章 応用編・トラシュ（S21〜31）**: トラシュ3枚（置き場所から疑う表／量の両極／「置いてあるのに効かない」）→ 管理・構成3枚（フォルダ構成／ルール→index→個別ファイルの段階的開示=OKF／再利用セット）→ 応用技術4枚（RAG／外部コネクター=Gmail例／スキル／メモリ）
- **4章 事例（S32〜37）**: ①規約徹底 ②レポートテンプレの一貫性 ③暴走防止（Replit 2025年7月本番DB削除。Cursor・Gemini CLIも言及）④初歩ミスの「2回指摘したらルール昇格」＋closing。**「ルールファイル＝助言、確実に止めるのは権限設定」の区別を絶対に曖昧にしない**（公式明言事項）
- **付録（S38〜71）34枚**: 5項目各論・4観点・構造化・5層表を移設。一次資料表4枚（S67〜70。S70がルールファイル/事故事例の出典）
- 用語統一: 「置き場所」「都度・条件付き・必ず」「ルールファイル（CLAUDE.md 等）」「正本」。9枚目の「AIは優秀な新入社員」比喩と接続

## 4. 品質プロセス（ループエンジニアリング）

- 採点表: リポジトリ直下 `rubric.md`（7項目・目標全8+。ユーザーレビューを項目化したもの）
- 記録: `loop-log.md` の「ループ2」節。スコア推移 **52/70 → 62/70（全項目8+）** で終了。maker≠grader（採点は別エージェント、作り手の意図は渡さない）
- 機械ゲート（毎回必須）: deck.json JSONパース → `build_deck.py <deck> --html` → `build_deck.py <deck>`（PPTX）→ `unzip -t` → python-pptx 再パース

## 5. 修正の手順（次セッションはこれに従う）

1. worktree `loop/ce-ch2-4-restructure` ブランチで `deck.json` を修正（新規worktree可。pipelineがmainに自動コミットするため main 直編集は不可）
2. `python3 tools/build_deck.py decks/ai-eng-02-context-engineering --html` → `tools/preview_deck.py <deck> <番号>` で目視 → PPTX 生成と検証
3. コミット・プッシュ（git基本操作は確認不要、revert/reset/force pushのみ事前確認——ユーザー明言）
4. **v2閲覧コピーへ反映**: deck.json と assets を `decks/ai-eng-02-context-engineering-v2/` にコピーし、同フォルダを対象に再ビルド＋preview再生成（9枚目はPR #8の内容が合流済みなので上書きに注意——#7ブランチ単体のdeck.jsonはS9が旧版。コピー後に `git show worktree-ce-slide9-newhire-vs-veteran:decks/ai-eng-02-context-engineering/deck.json` のS9(index 8)とSVGを再合流させること）
5. ユーザーのページ番号指摘は「v2の番号」基準で解釈する

## 6. 事実・出典（創作禁止の裏取り済み事実）

デッキ内の事実主張はすべて S67〜70 の一次資料表に対応。特に:
- CLAUDE.md 公式ベストプラクティス: 追記基準「同じミスを2回した／同じ訂正をまた入力しようとした」・200行未満推奨・具体的に書く・**CLAUDE.mdはadvisoryで強制はhook/権限設定**・矛盾ルールはランダムに従う（出典: code.claude.com/docs/en/memory, /best-practices, /hooks, /settings）
- 暴走実例: Replit 2025年7月（コードフリーズ指示下で本番DB削除、経営幹部約1,200人・企業約1,190社分。The Register 2025-07-21 / Fortune / AI Incident DB #1152）・Cursor YOLOモード 2025年6月・Gemini CLI 2025年7月（issue #4586）。**日付・規模はこの範囲を超えて書かない**
- OKF: knowledge/okf/ 配下に仕様まとめあり。1コンセプト1ファイル・index.md・frontmatter・段階的開示

## 7. 図版（SVG）の落とし穴

- `<style>` は必ず `text{font-family:'Hiragino Sans',...}` を **text要素セレクタ**で（クラス任せは明朝体化）
- 矢印の marker-end は **1パス1矢頭**（複数サブパスに1つだと最後しか描画されない）
- canvas 1024×740、最小フォント16px、色はテーマパレット（warm-terracotta）のみ

## 8. その他の運用メモ

- GitHub: アクティブアカウントが yuya-fujita-1201 なら `gh pr create` がそのまま通る（Cytra-yuya-fujita だと不可）
- メモリ（~/.claude/projects/.../memory/ce-deck-status.md）に本状況の要約あり。状況が動いたら更新すること
- PPTXにスピーカーノートを入れない（Keynote互換性）。notesフィールドはHTML専用
