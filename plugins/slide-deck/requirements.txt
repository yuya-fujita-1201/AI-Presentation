# slide-deck プラグインの Python 依存関係
# 導入: pip install -r requirements.txt
# もしくは /slide-deck:setup（tools/setup_deps.py）を使う
# PEP 668 (externally-managed-environment) で失敗する場合は venv を作るか、
# tools/setup_deps.py 実行時に案内される --user / --break-system-packages を検討する

python-pptx>=1.0,<2.0

# 任意: 画像からのテーマ抽出（theme_from_sample.py）、一覧サムネイル（contact_sheet.py）
# python-pptx が同梱依存として通常インストールするが、明示したい場合は以下を有効化
Pillow>=10

# 任意: プレビューPNG生成（preview_deck.py / check_layout.py / contact_sheet.py）
# インストール後に `playwright install chromium` の実行も必要
# playwright>=1.40

# 任意: PDFからのテーマ抽出（theme_from_sample.py --pdf）
# pymupdf>=1.24
